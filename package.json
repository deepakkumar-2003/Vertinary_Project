{
  "name": "veterinary-farm-management-system",
  "version": "1.0.0",
  "description": "Advanced Veterinary Clinic, Dairy & Farm Management System",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build && cd ../server && npm run build"
  },
  "keywords": ["veterinary", "farm", "dairy", "management"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
